class Controller {
  constructor(file) {
    this.file = file;
  }

  crop(crop) {
    new Promise((resolve, reject) => {
      resolve(result);
      reject(err);
    });
  }

  reduceQuality(quality) {
    new Promise((resolve, reject) => {
      resolve(result);
    });
  }


}
